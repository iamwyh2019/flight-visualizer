/* Visualize view — stub for a later stage.
 *
 * This will load the combined flights.geojson (all flights) and render them on
 * the shared FlightMap. Kept intentionally empty for Stage 1 Step 1. */
(function () {
  // Placeholder. Future: fetch('/flights.geojson') -> FlightMap.drawCollection(...)
})();
